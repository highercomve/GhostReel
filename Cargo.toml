[workspace]
resolver = "2"
members = ["crates/ghostreel-core", "crates/ghostreel-cli", "src-tauri", "crates/ghostreel-asr", "crates/ghostreel-llm"]
# spikes/ are throwaway experiments with their own workspace (and CUDA builds).
exclude = ["spikes"]

[workspace.package]
version = "0.4.2"
edition = "2024"
license = "MIT"

[profile.release]
debug = false
